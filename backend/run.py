#!/usr/bin/env python3
"""Dev entry point — runs the FastAPI server with auto-reload."""

import uvicorn

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
    )
