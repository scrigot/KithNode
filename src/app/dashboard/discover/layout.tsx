import { NetworkNav } from "@/components/network-nav";
export default function DiscoverLayout({ children }: { children: React.ReactNode }) { return <div className="min-h-full"><NetworkNav />{children}</div>; }
