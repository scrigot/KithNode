import { NetworkNav } from "@/components/network-nav";
export default function ContactsLayout({ children }: { children: React.ReactNode }) { return <div className="min-h-full"><NetworkNav />{children}</div>; }
