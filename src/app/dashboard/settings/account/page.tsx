import { auth } from "@/lib/auth";
import { SignOutButton } from "../../sign-out-button";

export default async function AccountSettingsPage() {
  const session = await auth();
  return <div className="mx-auto max-w-3xl p-5 sm:p-8"><h2 className="font-heading text-xl font-semibold text-text-primary">Account & privacy</h2><p className="mt-1 text-base text-text-secondary">Review the identity and safety boundary used by this workspace.</p><dl className="mt-6 divide-y divide-white/[0.08] border-y border-white/[0.08] bg-card"><div className="grid gap-1 px-4 py-4 sm:grid-cols-[180px_1fr]"><dt className="text-sm font-bold text-text-secondary">Signed in as</dt><dd className="text-base text-text-primary">{session?.user?.email || "Unknown account"}</dd></div><div className="grid gap-1 px-4 py-4 sm:grid-cols-[180px_1fr]"><dt className="text-sm font-bold text-text-secondary">Automation boundary</dt><dd className="text-base leading-6 text-text-primary">KithNode may analyze and propose. Sending messages, changing profiles, and submitting applications always remain user-controlled.</dd></div><div className="grid gap-1 px-4 py-4 sm:grid-cols-[180px_1fr]"><dt className="text-sm font-bold text-text-secondary">Private career data</dt><dd className="text-base leading-6 text-text-primary">Applications, resumes, LinkedIn copies, and connected-account tokens are scoped to this account and are not included in the public demo.</dd></div></dl><div className="mt-6"><SignOutButton /></div></div>;
}
